# Архив параллельных состояний фронтенда

Единственный источник правды по фронту — `public/index.html` + `public/style.css`
в текущем `main`. Здесь лежат более ранние/параллельные варианты, чтобы их
не путать с рабочим кодом (расхождение №03 аудита редизайна).

## `design-export-2026-08/`
22 PNG-макета (`turn-05..26`) обзорного дизайн-документа + пропатченная копия
`public/index.html` / `public/style.css` того периода («CSS-патч уже применён»).
Справочный материал по экранам, НЕ для переноса как есть.

## Ветка `redesign-apple-hig` (в origin, не смёржена)
Крупный параллельный рефактор (−27k строк) — заморожен. Редизайн ведётся от
`main` пофазно (см. `docs/`), эта ветка как ориентир не используется.
Удалять/мёржить — по отдельному решению.
