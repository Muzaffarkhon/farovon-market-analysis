# Хендофф — продолжение работы в другом чате / на другом ноуте

**Создан:** 2026-09-24 (после `HANDOFF_2026-09-22.md`)
**Проект:** Farovon Market Analysis («Обзор рынка вознаграждений»)
**Репозиторий:** https://github.com/Muzaffarkhon/farovon-market-analysis
**Ветка этой серии:** `redesign/react` → смёржена в `main` (коммит `95b51c0`)
**Стек:** Node/Express + Turso (libSQL). Два клиента: старый vanilla JS (`client/`, на `/`) и новый React (`web/`, на `/new`) — живут рядом на одном сервере, общая сессия.

---

## 1. Что сделано этой серией

### 1.1. Довели до конца незавершённую работу в новом клиенте (`web/`)

С прошлой сессии в рабочей копии копилось много несохранённых правок нового клиента — довели, проверили (тесты, типы, сборка) и закоммитили одним коммитом (`95b51c0`), затем смёржили `redesign/react` в `main` и запушили:

- Единый диалог подтверждения (`Confirm`) вместо `window.confirm` — с «опасным» вариантом для необратимых действий.
- Сортировка кликом по заголовку таблиц (`SortTh` + `useSort`) — подключена в пользователях, периодах, сотрудниках, бенчмаркинге.
- Модальный вариант диалогового окна (`Sheet variant="modal"`) — для форм, которые нужно читать, а не пролистывать.
- Анкета грейдирования — варианты ответа под перемешиваемыми буквами вместо баллов (защита от подглядывания и от «я поставил третий вариант» до итога комиссии).
- **Окно «разбивка по комиссии»** (`CommitteeBreakdownSheet`) — было создано в прошлой сессии, но нигде не подключалось (падала сборка: `declared but never read`). Довели: подключили к `GradingScreen`, открывается по клику на статус должности у тех, кто управляет грейдированием.
- На сервере: эндпоинты `restore-evaluation` и `committee-breakdown`, таблица `grading_reset_backup` (откат последнего сброса оценки).

### 1.2. Перенесли из старого клиента в новый: рассылка через Telegram

Раздел «Администрирование → Рассылка» (`web/src/features/admin/broadcast/`):
- Выбор получателей — поиск по ФИО/подразделению, «выбрать всех показанных».
- Текст сообщения (лимит 3500 символов), галочка «с кнопкой открыть приложение».
- Подтверждение перед отправкой (нельзя отменить после отправки).
- История рассылок с раскрытием — кто получил, кто нет.
- Бэкенд не трогали — эндпоинты `/admin/broadcasts*` уже существовали.

### 1.3. Перенесли из старого клиента в новый: журнал изменений

Раздел «Администрирование → Журнал изменений» (`web/src/features/admin/service/`):
- Таблица всех административных действий (кто, когда, что сделал).
- Поиск по логину/деталям, фильтр по типу действия, сортировка по колонкам, постраничный вывод по 50.
- Бэкенд не трогали — эндпоинт `/admin/audit-log` уже существовал.

### 1.4. Перенесли из старого клиента в новый: справочники (компании/должности/сегменты/регионы) + объединение дублей

Раздел «Администрирование → Справочники» (`web/src/features/admin/dictionary/`):
- Вкладки Компании / Должности / Сегменты / Регионы, поиск, добавление и правка записей — сегмент/регион у компании выбираются из справочника (не свободный ввод — иначе снова разъедутся варианты написания), направления отмечаются чекбоксами, у должностей — вилка «Оклад Фаровона».
- Удаление сначала считает объём затронутых данных (анкеты, участники рынка и т.п.) через `GET /admin/dictionary/:kind/usage` и показывает диалог подтверждения с этой цифрой, только потом реально удаляет.
- Бэкенд (`src/controllers/dictionaryController.js`, маршруты `/admin/dictionary/*`) не трогали — он уже существовал, использован как есть.
- **Кнопка «Объединить дубли»** (для компаний и должностей, право `service:edit`) — тот же инструмент, что в старом клиенте лежал в «Сервисных утилитах» (`POST /admin/maintenance` с разными `taskType`): список написаний с числом использований, подсказки «похоже: X, Y» от `find_similar_names` (кликабельны — сразу отмечают обе записи), выбор «сделать основным», диалог-подтверждение (действие необратимо), после — реальное слияние на сервере (`merge_companies`/`merge_positions`): все упоминания в анкетах, участниках рынка, штатке и выборе компаний по должностям переписываются на выбранное имя, дубль удаляется из справочника.
- Новые файлы: `web/src/api/dictionary.ts`, `web/src/api/maintenance.ts`, `web/src/features/admin/dictionary/{DictionaryScreen,MergeDuplicates,useDictionary,useMerge}.{ts,tsx}` + тесты.
- **Попутный фикс бэкенда**: при слиянии должностей (`merge_positions` в `adminController.js`) поле `surveys.pos_their` (должность у компании-конкурента) не обновлялось на новое имя, хотя при обычном переименовании должности (`dictionaryController.save`) — обновляется. Несогласованность устранена, теперь оба пути ведут себя одинаково.
- Не переносил: справочник сотрудников из 1С (`StaffScreen`) и оргструктура (`DivisionsScreen`) — они уже были перенесены раньше отдельными экранами, хоть и относятся к тому же смысловому разделу «Справочники» старого клиента.
- Проверено вживую в браузере на dev-данных (реальная копия базы, не синтетика): список компаний и должностей грузится, вкладки переключаются, модалка объединения дублей открывается, подсказки кликабельны, кнопка «Объединить N → 1» появляется при выборе. Само слияние в проверке не выполнял (необратимо) — только автотестами (см. ниже).

### 1.5. Перенесли из старого клиента в новый: онбординг, живое обновление, Telegram-профиль

Три пункта из §2 (были в «Существенное»):

- **Онбординг** (`web/src/features/auth/UnitPickScreen.tsx`, `AssignmentPendingScreen.tsx`) — если у только что вошедшего пользователя нет назначенных подразделений (`needsUnitPick`), `RequireAuth.tsx` блокирует доступ к остальному приложению и показывает полноэкранный выбор подразделений с поиском (использует `/auth/set-units`); для `dir_head`/`head` (им подразделение назначает администратор, самим выбрать нельзя — проверяется и на сервере) — экран-заглушка «Назначение ещё не выполнено» с кнопкой «Проверить снова» (`needsAssignment`, перечитывает сессию). Отметка «прошёл онбординг» (`/auth/onboarded`) ставится автоматически один раз, как только человек первый раз доходит до рабочего экрана — обучающего тура (coach-marks) старого клиента в новом нет, эта часть сознательно не перенесена (её не было в исходном запросе на перенос).
- **Живое обновление** (`web/src/features/shell/useLiveRefresh.ts`, подключено в `Shell.tsx`) — раз в 45 сек, пока вкладка видна, сверяет подпись данных (`GET /live-signature`, бэкенд не трогали) и при расхождении обновляет все открытые на экране запросы. В отличие от старого клиента (там перерисовывались только дашборд и админка) здесь обновляется любой открытый экран — тот же приём, что уже был у кнопки «Обновить» и жеста «потянуть вниз».
- **Telegram-профиль** (`web/src/features/auth/ProfileScreen.tsx`, `TelegramLinkSheet.tsx`) — новый экран «Профиль» (пункт в меню «Ещё»/бургер-меню), в нём: смена ФИО (`/auth/change-name`) и блок Telegram — «Привязать» открывает инструкцию (команда `/link` в самом боте + запасной вариант со ссылкой-кодом на 10 минут, `/telegram/link` + `/telegram/bot-info`), «Отвязать» — с подтверждением (`/telegram/unlink`).
- Проверено вживую: под `dev.hrbp` (пустые подразделения) сработал редирект на выбор подразделений, выбор сохранился и пропустил на главный экран; на «Профиль» под `dev.admin` — форма ФИО отрисована, попытка привязать Telegram корректно показала ошибку сервера («бот не подключён» — в dev-окружении токен бота не настроен, это ожидаемо).

---

## 2. Полный аудит: что ещё не перенесено из старого клиента в новый

Сверили все серверные `/api/*` маршруты с тем, что реально вызывается из `web/`. Список подтверждён построчно (не по памяти).

**Существенное:**
1. ~~Онбординг нового пользователя~~ — перенесено в этой серии, см. §1.5.
2. ~~Живое обновление данных~~ — перенесено в этой серии, см. §1.5.
3. ~~Привязка/отвязка Telegram из профиля~~ — перенесено в этой серии, см. §1.5.

**Административное:**
4. **Конструктор ролей** (`/admin/roles`, `/admin/roles/:key/rename`, `/admin/roles/:key/delete`) — в новом клиенте можно только редактировать права существующих ролей, создать/переименовать/удалить роль нельзя.
5. ~~Справочники (компании/должности), объединение дублей~~ — перенесено в этой серии, см. §1.4.
6. **Обслуживание и статус данных** (`/admin/maintenance`, `/admin/data-status`, `/admin/import-survey`) — служебные кнопки «запустить обслуживание», «статус загруженных данных», массовый CSV-импорт анкеты через админку.
7. **Бейдж непрочитанных** в админском инбоксе поддержки (`/admin/support/unread-count`) — сам инбокс работает, просто нет счётчика-уведомления рядом с пунктом меню.
8. **Смежные группы подразделений** (`/admin/divisions/adjacent-group*`) — объединение похожих подразделений (разные площадки одной сети) в одну карточку сравнения зарплат. Отображается в оргструктуре как карточка «Смежная · N площ.», задаётся полем `group_key` у подразделения. Ни отображения, ни редактирования в новом клиенте нет.

**Второстепенное:**
9. **Выгрузка в CSV** — у старого дашборда была кнопка экспорта сырых данных (`/dashboard/export-csv`, `/benchmarks/export`). Выгрузка реестра (`/registry/export`) уже перенесена, эти — нет.
10. **Персональные веса по должности** в бенчмаркинге (`/benchmarks/position-weights`) — сейчас можно менять веса источников целиком, override по одной конкретной должности нельзя.

**Полностью перенесено и работает:** анкета сбора данных, реестр, дашборды (кроме CSV-экспорта), координация, грейдинг с комиссией, риски, бенчмаркинг (кроме двух пунктов выше), поддержка (кроме бейджа), оргструктура (кроме смежных групп), рассылка, журнал изменений, справочники компаний/должностей/сегментов/регионов + объединение дублей, онбординг/выбор подразделений, живое обновление, Telegram-профиль.

---

## 3. Новая функция: «Пересмотр заработной платы»

**Важно:** более ранняя версия этого раздела (первая половина той же сессии) описывала самодельную упрощённую заявку («заявка на изменение зарплаты», права `salary:*`) — оказалось, что в проекте уже был согласованный с заказчиком документ на эту функцию, который тогда не нашли. Та версия **полностью удалена** (код, таблицы `salary_*`, права) и переделана заново по документу. Текст ниже — про финальную, верную версию.

**Спецификация:** [docs/superpowers/specs/2026-09-22-comp-review-design.md](../superpowers/specs/2026-09-22-comp-review-design.md), статус «согласован по ключевым вопросам 22.09.2026». Читать его целиком перед любой доработкой этой функции — здесь только выжимка.

**Маршрут:** Черновик (HRBP) → Проверка C&B (может вернуть на доработку с комментарием) → Согласование HRD (может отклонить всю заявку) → Голосование комиссии **большинством от зафиксированного состава** — пропускается для типа заявки «выход из стажировки» → Кадровик вносит в 1С → Закрыта. Решения C&B/HRD — на уровне всей заявки; решения комиссии — **по каждому сотруднику независимо** (заявка — «опросник» на несколько сотрудников разом, отклонённый выпадает, остальные идут дальше; заявка закрывается, когда по каждому есть финальное решение).

**Ключевые решения, зафиксированные в документе (§9) — не пересматривать без явного запроса:**
1. HRD — конкретный человек (Сатторов Илхомчон Ахмадчонович), не роль системы; право `comp:approve_hrd` выдаётся ему персонально через «Роли и доступы». Ему же можно дополнительно выдать `comp:vote` — тогда он и голосует в комиссии.
2. Режим голосования комиссии переключаемый (открытое — голоса видны сразу; закрытое — видно только «проголосовал/нет» до итога) — настройка в «Администрирование → Пересмотр ЗП — комиссия», действует на новые заявки.
3. Большинство считается **от состава комиссии**, не от числа проголосовавших. Состав фиксируется («снимок») в момент, когда HRD передаёт заявку в комиссию (колонка `comp_requests.committee_snapshot`, JSON-список логинов) — более поздние изменения живого списка `comp_committee_members` на уже идущее голосование не влияют. Не проголосовавшие видны C&B/администратору всегда, независимо от режима; кнопка «Напомнить» — вручную (C&B/админ) и автоматически раз в 3 дня без движения — через уже существующий ежедневный Vercel Cron (`vercel.json` → `/api/cron/reminders`), не отдельный крон.

**Правило 6 месяцев (§2):** без истории пересмотров — всегда можно; меньше 6 месяцев с последнего — только с кодом основания «удержание/контр-оффер» или «уникальный случай» и обязательным текстом обоснования, иначе система отказывает при добавлении сотрудника в черновик.

**Данные:** `dictionary_positions.pay_from/pay_to` — источник «вилки» для положения оклада в вилке (compa-position); `benchmarkService.compare()` — источник рыночной медианы для compa-ratio на шаге C&B (если сопоставления с бенчмаркингом нет — поле остаётся пустым, C&B заполняет вручную). «Текущий оклад» сотрудника — не колонка на `staff_directory` (та целиком пересоздаётся при каждом импорте из 1С), а последняя запись в новой таблице `comp_review_history` по паре `(unit, fio)`, которая переживает реимпорт; туда же пишется история для правила 6 месяцев.

**Осознанно не сделано (нужна внешняя инфраструктура/решение):**
- **Вложения файлов** (документ-основание, до 5 файлов, PDF/фото) — по документу должны храниться в Vercel Blob, которого в проекте нет. Поле `basis_document` пока текстовое (ссылка/название вручную), самой загрузки файлов нет.

**Что сделано:**
- Backend: 8 новых таблиц (`comp_requests`, `comp_request_employees`, `comp_variable_pay`, `comp_variable_pay_kinds`, `comp_committee_members`, `comp_committee_votes`, `comp_settings`, `comp_activity`, `comp_review_history`) в `src/db/migrate.js` (миграция `createCompReview`, дропает старые `salary_*`); чистая бизнес-логика (правило 6 месяцев, рост/compa-ratio/вилка, итог голосования большинством от снимка состава) в `src/services/compReviewLogic.js` — 13 юнит-тестов в `test/compReviewLogic.test.js`, без БД; БД-слой в `src/services/compReviewService.js`; контроллер `src/controllers/compReviewController.js`; маршруты `/api/comp/*`. 6 новых прав (`comp:submit/review_cb/approve_hrd/vote/payroll/admin`) в `capabilities.js` — `comp:submit`+`comp:review_cb` по умолчанию у роли `cb`, `comp:submit` у `hrbp`, остальные — только персонально. Полный сквозной прогон через сервисный слой (черновик → все этапы → закрытие, обе ветки — с комиссией и без) сделан вручную в этой сессии, багов найдено и исправлено два (см. код).
- Новый клиент (`web/`): пункт «Пересмотр ЗП» в меню; реестр с вкладками «Ждут меня / Мои / Все актуальные / Закрытые» (`web/src/features/compReview/RequestsListScreen.tsx`); экран заявки с карточками сотрудников, действиями по каждому этапу и лентой событий/комментариев (`RequestScreen.tsx`, `EmployeeCard.tsx`, `EmployeeAddForm.tsx`); админский экран состава комиссии и режима голосования (`web/src/features/admin/compReview/CompCommitteeScreen.tsx`).
- Таблица сотрудников заявки в ТЗ описана как «опросник» (сотрудники колонками, поле — строка, кнопка «→ скопировать вправо во все»). Сделано вертикальным списком карточек — тот же набор полей и действий, но не построчная сетка и без кнопки копирования между сотрудниками. Функционально эквивалентно, визуально отличается от вайрфрейма в документе.

**Открытое:**
- Тот же нюанс с видимостью в меню, что раньше — пункт меню требует хотя бы одного из прав `comp:*`, чистое членство в комиссии (без права) его не даёт. При добавлении члена комиссии — выдать ему `comp:vote` через «Роли и доступы», иначе он не увидит раздел (хотя голосовать технически сможет по прямой ссылке).
- Файлы-вложения — см. выше, нужно решение по Vercel Blob.
- Прод-QA не делали — только автотесты, ручной сквозной прогон через сервисный слой и сборка.

## 4. Прочее открытое

- Список из §2 не приоритизирован — переносить по одному, по мере запроса.
- Прод-QA после мерджа `redesign/react` → `main` не делали — правки не проверялись вживую на проде.
